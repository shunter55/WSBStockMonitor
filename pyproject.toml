[project]
name = "wsb-stock-monitor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "praw>=7.7.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "api/run/index.py"
